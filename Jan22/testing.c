# include <stdio.h>
# include <stdbool.h>

int main( void )
{
    /*
    int x;
    x = 5;

    // if statement details
    if( x > 0 )
    // When no braces follow the if statement then only the next statement 'belongs' to the if.
    //  In C everything except for 0 is TRUE.
    //
    {
        printf("Positive\n");
        printf("x is definately psitive!/n");   
    }
    */

    bool y;
    y = true;
    
    if( y = true )
    {
        printf("y is true!\n")
    }

}
