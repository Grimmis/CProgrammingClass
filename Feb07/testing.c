#include <stdio.h>
#define SIZE 5

int main( void )
{
    /*
    short s = 0;
    int i = 0;
    int arr01[5];
    int arr02[] = { 1, 2, 3, 4, 5, 6 };

    printf( "Size of short: %lu\n", sizeof(s) );
    printf( "Size of int: %lu\n", sizeof(i) );
    printf( "Size of arr01[5]: %lu\n", sizeof(arr01) );
    printf( "Size of arr02: %lu\n", sizeof(arr02) );

    size_t len = sizeof( arr02 ) / sizeof( int );
    printf( "Length of arr02: %lu\n", len);
    */
    /*
    int a[2][2];
    // or
    int b[][] = { { 1, 2 }, { 3, 4 } };
    // or
    int c[][] = { { 1, 2, 3 },
                  { 4, 5, 6 },
                  { 7, 8, 9 }
                };
    */
    /*
    int array1[2][3] = { 1, 2, 3, 4, 5 };
    // equivilant to { { 1, 2, 3 }, { 4, 5, 0 } }
    int array2[2][3] = { { 1 }, { 3, 4 } };
    // equivilant to { { 1, 0 }, { 3, 4, 0 } }
    */
    /*
    int mat[][3] = { { 1, 2, 3 }, { 4, 5, 6 } };
    
    printf( "%s", "\n" );
    for( size_t i = 0; i < 2; i++ )
    {
        for( size_t j = 0; j < 3; j++ )
        {
            printf( "mat[i][j] = %d\n", mat[i][j] );
        }
        printf( "%s", "\n\n" );
    }
    */
    
}